# Run Matrix

Read and execute `RUN_MATRIX.md` in this directory. Follow every instruction in that file. Do not summarize it. Do not explain it. Execute it.

When a user says "run the matrix", "plan my app", or describes an app idea — read `RUN_MATRIX.md` and follow its instructions from the top.
